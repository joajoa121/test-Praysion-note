Praysion Note - Detail fixed topbar test

Changed files only:
- assets/app.css
- service-worker.js

What changed
1. Detail page TopBar is removed from normal flex/scroll flow.
2. On mobile/PWA, detail TopBar uses position:fixed at the viewport top.
3. Detail view becomes a separate fixed layer starting at 48px below the TopBar.
4. Only #detail-scroll scrolls inside that layer.
5. Existing list/archive TopBar collapse and anti-jitter rules are kept.
6. Existing keyboard/viewport guard JavaScript is kept unchanged as a fallback.
7. Service worker cache version: 1.0.83-detail-fixed-topbar

Test especially:
- Open prayer detail -> Edit -> focus title/body -> keyboard open -> scroll content.
- TopBar should remain at the top while content scrolls below it.
