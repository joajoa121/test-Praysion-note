Praysion Note - answer flow + PIN fix

Changed files only:
- index.html
- prayer.js
- settings.js
- bootstrap.js
- service-worker.js
- assets/app.css

PIN fix:
- Removed an invalid HTML <script> tag that was embedded inside bootstrap.js.
- bootstrap.js contains the click/keyboard event binding for the PIN keypad, so the syntax error prevented PIN input from working.
- Service worker cache version bumped to 1.0.81-pin-fix.

After upload:
1. Replace these files in the same paths in the GitHub repository.
2. Wait for GitHub Pages deployment to finish.
3. Close the installed PWA completely and reopen it.
4. If the old version persists, remove the installed PWA/site data once and reinstall from the updated Pages site.
